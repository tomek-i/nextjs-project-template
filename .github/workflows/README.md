# Workflows

## convert-todo-to-issues.yaml

This workflow converts `// TODO:` items in code into GitHub Issues.

## pr-title-emoji.yaml

This workflow will prefix the PR with an emoji based on the prefixed word.

## pr-merge-commit-emoji.yaml

This workflow will prefix the PR commit message when merged.
